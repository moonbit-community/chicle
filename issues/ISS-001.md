# ISS-001: Populate layout order for positioned children

## Metadata
- Type: bug
- Status: open
- Priority: 1
- Labels: tree, layout-metadata, upstream-parity, tests
- Assignee: unassigned
- Created: 2026-07-12
- Updated: 2026-07-12
- External ref: none

## Description
`Layout.order` is part of the public layout result and is intended to describe the relative rendering order of nodes. The pinned Rust Taffy implementation assigns source order to block, flex, and grid children, while the MoonBit implementation only initializes `order` to `0` in `Layout::zero()` and never overwrites it.

A runtime probe with a root and two leaf children currently reports `orders root=0 a=0 b=0`. This makes the public field unusable for consumers that rely on Taffy layout output to derive stable paint order.

## Design
Propagate source order at the point where each layout algorithm performs final child placement, matching the pinned Rust Taffy v0.5.2 behavior. Keep the root order at zero. Cover block, flex, grid, absolute-positioned children, hidden nodes, and cache restoration. Do not introduce a new z-index feature; this issue concerns the existing Taffy source-order contract only.

## Acceptance Criteria
- [ ] Sibling layouts receive the same relative `order` values as pinned Rust Taffy v0.5.2 for block, flex, grid, and absolute-positioned placement.
- [ ] The root layout retains `order == 0`.
- [ ] Recomputed and cache-restored layouts preserve the assigned order.
- [ ] Focused downstream-style regression tests fail before the fix and pass after it.
- [ ] `moon check`, `moon check --warn-list +unnecessary_annotation`, and `moon test --no-render --target all` pass.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-002, ISS-003
- Discovered from: none

## Notes
- 2026-07-12: Static search found no `order:` assignment under `src/tree` other than `Layout::zero()`.
- 2026-07-12: Runtime reproduction on MoonBit 2026-07-10 produced `orders root=0 a=0 b=0` for a root with two normal leaf children.
- 2026-07-12: Upstream assigns `item.order` during flex placement and analogous source-order values in block and grid placement.

## Close Notes
